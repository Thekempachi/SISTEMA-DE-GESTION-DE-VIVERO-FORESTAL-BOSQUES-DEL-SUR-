# Variables de entorno para el Sistema de Gestión de Vivero
# Copia este archivo a .env y configura los valores según tu entorno

# === MODO DEBUG ===
# APP_DEBUG=1 activa mensajes detallados de error y endpoints de diagnóstico
# APP_DEBUG=0 (o no definida) modo producción (recomendado para producción)
# IMPORTANTE: Solo usar APP_DEBUG=1 temporalmente para diagnóstico
APP_DEBUG=0

# === CONFIGURACIÓN DE BASE DE DATOS ===
# Estas variables se usan si prefieres variables de entorno en lugar de
# las constantes definidas en backend/php/conection.php
# En Hostinger, puedes configurar estas variables en hPanel → Environment Variables

DB_HOST=localhost
DB_NAME=vivero_db
DB_USER=vivero_user
DB_PASS=tu_password_seguro

# === NOTAS DE USO ===

# 1. En hosting compartido (Hostinger):
#    - Puedes configurar estas variables en hPanel → Environment Variables
#    - O editar directamente backend/php/conection.php con las constantes
#    - Las variables de entorno tienen prioridad sobre las constantes

# 2. Para desarrollo local:
#    - Copia este archivo a .env
#    - Configura los valores según tu entorno local
#    - Asegúrate de tener un servidor web (Apache/Nginx) con PHP 8.x

# 3. Seguridad:
#    - Nunca subas el archivo .env al repositorio
#    - Usa contraseñas fuertes para DB_PASS
#    - Desactiva APP_DEBUG en producción

# 4. Endpoints de diagnóstico (solo con APP_DEBUG=1):
#    - /backend/php/api/db_check.php: Verifica conexión a BD y estado del sistema
#    - /backend/php/api/set_password.php: Permite asignar contraseñas de forma segura

# 5. Migración de usuarios:
#    - El sistema soporta usuarios con contraseñas en texto plano
#    - Al iniciar sesión, las contraseñas se migran automáticamente a hash seguro
#    - Usa set_password.php para asignar nuevas contraseñas de forma segura

# === EJEMPLO PARA DESARROLLO LOCAL ===
# APP_DEBUG=1
# DB_HOST=localhost
# DB_NAME=vivero_dev
# DB_USER=root
# DB_PASS=

# === EJEMPLO PARA PRODUCCIÓN ===
# APP_DEBUG=0
# DB_HOST=tu_host_mysql
# DB_NAME=tu_base_datos
# DB_USER=tu_usuario
# DB_PASS=tu_password_real
